#include "main.h"
/**
 * _abs - Description: 'absolute value'
 *
 *@n: integer to use
 *
 * Return: return 0 (success)
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}

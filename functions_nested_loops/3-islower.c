#include<stdio.h>
/**
 * _islower - Entry point
 *
 * @c: integer
 *
 * Description: 'checks for lowercase character'
 *
 * Return: Return 1 (fail) or 0 (sccess)
*/
int _islower(int c)
{

int i;
int c;

for (i = 97; i <= 122; i++)
{
	if ((i < 97 )) or (( i > 122 ));
}
{
	putchar('0');
}
else if
{
	putchar( (c= char (i);) + '0');
}

putchar('\n');
return (0);
}
}

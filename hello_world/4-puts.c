#include<stdio.h>
intmain(void)
/*
 * main - Entry point
 * Description- C program that prits a string
 * Return: Always 0
 */
{
    puts("\"Programming is like building a multilingual puzzle");
    return (0);
}
